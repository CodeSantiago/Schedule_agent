"""Domain layer placeholder.

The domain layer will hold pure business logic (entities, value objects,
domain services) with no infrastructure dependencies. Kept intentionally
empty for this first slice — only schema is being scaffolded.
"""
