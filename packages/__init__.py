"""Barber Agent — Multi-tenant barber booking platform (greenfield).

This package hosts the long-lived, multi-tenant rebuild of the original
single-tenant bot that lives under `solo-tenant-bot/` (kept as legacy
reference only — DO NOT import from it).
"""
